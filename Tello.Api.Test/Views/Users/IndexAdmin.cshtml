@model List<UserGetDto>
@{
    ViewBag.Title = "Admins";
    int count = 0;
}

<div class="col-lg-9 col-11 mx-auto stretch-card">
    <div class="card">
        <div class="card-body">
            <p class="card-description">
                <a asp-action="createadmin" asp-controller="users" class="btn btn-outline-success">Create</a>
            </p>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Roles</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr class="text-light">
                                <td>@count</td>
                                <td>@item.Fullname</td>
                                <td>@item.Email</td>
                                <td>
                                    @foreach (var role in item.Roles)
                                    {
                                        <span>@role</span>
                                    }</td>
                                <td><a asp-action="editadmin" asp-route-id="@item.Id" class="badge badge-outline-danger">Edit</a> </td>
                                <td><a asp-action="deleteadmin" asp-route-id="@item.Id" data-name="admin" class="badge badge-outline-danger sweet-delete">Delete</a> </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>